- @rightbar ||= 'groups/sidebar'
- inside_layout 'application' do

  #maincolumn.rightbar{ :style => sidebar_space }
    = sidebar
    = render :partial => 'banner_large'

    #content
      - if title = yield( :title )
        #titlebox
          = title
      = yield

    %br{ :clear => "all" }
