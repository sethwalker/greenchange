<%= return_to_page(@page) %>

<ul>
  <% for version in @wiki.versions %>
  <li>
    <%=link_to "version %s"%version.version, page_url(@page, :action => "version", :id => version.version) %>
    <%=link_to "(diff)", page_url(@page, :action=>'diff', :from => version.version.to_i-1, :to => version.version) if version.version > 1 %>,
    created <i><%= full_time version.updated_at %></i> by <b><%= link_to_user(version.user_id)%></b>
  </li>
  <% end %>
</ul>
