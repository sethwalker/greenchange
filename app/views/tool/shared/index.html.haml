%h1
  = controller.controller_name.pluralize.titleize
.content-list.main
  = will_paginate(@pages)
  %ul.list
    = render :partial => 'pages/list/page', :collection => @pages
  = will_paginate(@pages)
