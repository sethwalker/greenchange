.icon{ :class => (person.online? ? 'online':'')}
  = link_to person.display_name, person_url(person), html_options_for_icon_for( person, :size => (local_assigns[:size] ||'standard'), :class => 'name-bottom' )
  -#-if scoped_by_context? == @me  && scoped_by_context?
    %br
    .toolbar
      =link_to 'message'
      |
      =link_to 'remove'
