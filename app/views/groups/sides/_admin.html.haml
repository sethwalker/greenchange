- if logged_in? && current_user.may_admin?(@group)
  .sidesection
    .sidehead
      Administer Group
    = link_to 'edit settings', groups_url(:action => 'edit', :id => @group, :path => nil), :class => 'action'
    = link_to 'view requests', groups_url(:action => 'requests', :id => @group, :path => nil), :class => 'action'
    = link_to 'destroy group', groups_url(:action => 'destroy', :id => @group, :path => nil), :class => 'action'
